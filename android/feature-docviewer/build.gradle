plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.morselink.feature.docviewer"
    compileSdk = 35

    defaultConfig {
        minSdk = 23
        targetSdk = 35
    }

    sourceSets["main"].java.srcDirs("src/main/java/kotlin")

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17
        targetCompatibility = JavaVersion.VERSION_17
    }
    kotlinOptions { jvmTarget = "17" }
}

dependencies {
    implementation(libs.androidx.appcompat)
    implementation(libs.androidx.core.ktx)
    implementation(libs.androidx.recyclerview)

    // Apache POI (Office docs). On low-end Android builds this is heavy and
    // requires core-library desugaring; the converter is optional and the
    // runtime falls back to ACTION_VIEW if POI is unavailable.
    // implementation("org.apache.poi:poi-ooxml:5.2.5")
}
